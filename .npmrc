# legacy-peer-deps: workaround per un bug noto di npm 10.x (arborist "Cannot read
# properties of null (reading 'edgesOut')") che si verifica risolvendo i peer
# dependency di Angular/Vitest. Rimuovere quando si aggiorna npm e il bug è risolto.
legacy-peer-deps=true
